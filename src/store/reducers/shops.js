import { createSlice } from '@reduxjs/toolkit'

const initialState = null

const shopsSlice = createSlice({
  name: 'shops',
  initialState,
  reducers: {}
})

export const { reducer, actions } = shopsSlice
